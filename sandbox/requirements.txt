# Code-interpreter sandbox runtime (Phase 4).
# Deliberately small: a web shim to receive jobs + the data-analysis
# stack the model is most likely to reach for. No network/DB clients —
# this container runs on an internal-only docker network with no
# internet and no access to Postgres/Redis.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pandas==2.2.3
numpy==2.2.1
matplotlib==3.10.0
openpyxl==3.1.5
python-dateutil==2.9.0.post0
tabulate==0.9.0
